# Getting Started

Follow these steps:

1. Sign up and get your API key.
2. Review the [API Reference](../apis/sample@v1).
3. Make your first request:

```bash
curl https://api.example.com/hello \
  -H "Authorization: Bearer <your-api-key>"
```
